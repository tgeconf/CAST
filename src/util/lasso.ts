// import classifyPoint from 'robust-point-in-polygon'
import { ICoord } from './ds'

export default class Lasso {
    static CLOSE_DIS: number = 75;

    origin: ICoord;
    public initContainer(containerId: string) {

    }
}